/**
 * Shared Value JSON helpers and type definitions used across Vizij wasm wrappers.
 */
const DEFAULT_VEC2 = [0, 0];
const DEFAULT_VEC3 = [0, 0, 0];
const DEFAULT_VEC4 = [0, 0, 0, 0];
const DEFAULT_QUAT = [0, 0, 0, 1];
const DEFAULT_COLOR = [0, 0, 0, 0];
const DEFAULT_SCALE = [1, 1, 1];
const EMPTY_STRING = "";
const NORMALIZED_TYPE_TAGS = new Set([
    "float",
    "bool",
    "text",
    "vec2",
    "vec3",
    "vec4",
    "quat",
    "colorrgba",
    "vector",
    "enum",
    "record",
    "array",
    "list",
    "tuple",
    "transform",
]);
function coerceNormalizedType(value) {
    const raw = value.type;
    const lower = raw.toLowerCase();
    return NORMALIZED_TYPE_TAGS.has(lower) ? lower : null;
}
function isRecord(value) {
    return typeof value === "object" && value !== null;
}
function toFiniteNumber(value) {
    if (typeof value === "number" && Number.isFinite(value)) {
        return value;
    }
    if (typeof value === "string" && value.trim().length > 0) {
        const parsed = Number(value);
        return Number.isFinite(parsed) ? parsed : undefined;
    }
    return undefined;
}
function coerceNumericArray(values, fallback = 0) {
    if (!Array.isArray(values))
        return undefined;
    return values.map((entry) => toFiniteNumber(entry) ?? fallback);
}
function coerceTuple(values, defaults) {
    if (!Array.isArray(values))
        return undefined;
    const result = defaults.slice();
    for (let i = 0; i < defaults.length; i += 1) {
        const num = toFiniteNumber(values[i]);
        if (num !== undefined) {
            result[i] = num;
        }
    }
    return result;
}
function normalizedValueAsNumber(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "float": {
            const num = Number(value.data);
            return Number.isFinite(num) ? num : undefined;
        }
        case "bool":
            return value.data ? 1 : 0;
        case "vec2":
        case "vec3":
        case "vec4":
        case "quat":
        case "colorrgba":
        case "vector": {
            const first = value.data[0];
            const num = Number(first);
            return Number.isFinite(num) ? num : undefined;
        }
        case "transform": {
            const first = value.data.translation[0];
            const num = Number(first);
            return Number.isFinite(num) ? num : undefined;
        }
        case "enum":
            return normalizedValueAsNumber(value.data[1]);
        case "array":
        case "list":
        case "tuple":
            return value.data.length > 0
                ? normalizedValueAsNumber(value.data[0])
                : undefined;
        default:
            return undefined;
    }
}
function normalizedValueAsNumericArray(value, fallback) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "float": {
            const num = Number(value.data);
            return [Number.isFinite(num) ? num : fallback];
        }
        case "bool":
            return [value.data ? 1 : 0];
        case "vec2":
        case "vec3":
        case "vec4":
        case "quat":
        case "colorrgba":
        case "vector":
            return value.data.map((entry) => {
                const num = Number(entry);
                return Number.isFinite(num) ? num : fallback;
            });
        default:
            return undefined;
    }
}
function normalizedValueAsTransform(value) {
    const type = coerceNormalizedType(value);
    if (type !== "transform")
        return undefined;
    const { translation, rotation, scale } = value.data;
    return {
        translation: translation.map((n) => Number(n) || 0),
        rotation: rotation.map((n) => Number(n) || 0),
        scale: scale.map((n) => Number(n) || 0),
    };
}
function normalizedValueAsVec3(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "vec3":
            return [...value.data];
        case "vec4": {
            const [x, y, z] = value.data;
            return [x, y, z];
        }
        case "quat": {
            const [x, y, z] = value.data;
            return [x, y, z];
        }
        case "colorrgba": {
            const [r, g, b] = value.data;
            return [r, g, b];
        }
        case "vector": {
            const data = value.data;
            return [data[0] ?? 0, data[1] ?? 0, data[2] ?? 0];
        }
        case "transform": {
            const pos = value.data.translation;
            return [pos[0] ?? 0, pos[1] ?? 0, pos[2] ?? 0];
        }
        case "enum":
            return normalizedValueAsVec3(value.data[1]);
        case "array":
        case "list":
        case "tuple":
            return value.data.length > 0
                ? normalizedValueAsVec3(value.data[0])
                : undefined;
        case "float": {
            const num = value.data;
            return [num, num, num];
        }
        case "bool":
            return value.data ? [1, 1, 1] : [0, 0, 0];
        default:
            return undefined;
    }
}
function normalizedValueAsVector(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "vector":
            return value.data.slice();
        case "vec2":
            return [...value.data];
        case "vec3":
            return [...value.data];
        case "vec4":
            return [...value.data];
        case "quat":
            return [...value.data];
        case "colorrgba":
            return [...value.data];
        case "transform": {
            const data = value.data;
            return [
                data.translation[0] ?? 0,
                data.translation[1] ?? 0,
                data.translation[2] ?? 0,
                data.rotation[0] ?? 0,
                data.rotation[1] ?? 0,
                data.rotation[2] ?? 0,
                data.rotation[3] ?? 0,
                data.scale[0] ?? 1,
                data.scale[1] ?? 1,
                data.scale[2] ?? 1,
            ];
        }
        case "enum":
            return normalizedValueAsVector(value.data[1]);
        case "array":
        case "list":
        case "tuple":
            return value.data.flatMap((entry) => normalizedValueAsVector(entry) ?? []);
        case "float":
            return [value.data];
        case "bool":
            return [value.data ? 1 : 0];
        default:
            return undefined;
    }
}
function normalizedValueAsBool(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "bool":
            return value.data;
        case "float":
            return value.data !== 0;
        case "text":
            return value.data.length > 0;
        case "vec2":
        case "vec3":
        case "vec4":
        case "quat":
        case "colorrgba":
        case "vector":
            return value.data.some((entry) => Number(entry) !== 0);
        case "transform": {
            const { translation, rotation, scale } = value.data;
            return (translation.some((entry) => Number(entry) !== 0) ||
                rotation.some((entry) => Number(entry) !== 0) ||
                scale.some((entry) => Number(entry) !== 0));
        }
        case "enum":
            return normalizedValueAsBool(value.data[1]);
        case "record":
            return Object.values(value.data).some((entry) => normalizedValueAsBool(entry) ?? false);
        case "array":
        case "list":
        case "tuple":
            return value.data.some((entry) => normalizedValueAsBool(entry) ?? false);
        default:
            return undefined;
    }
}
function normalizedValueAsQuat(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "quat":
            return [...value.data];
        case "vec4": {
            const [x, y, z, w] = value.data;
            return [x, y, z, w];
        }
        case "vector": {
            const vec = value.data;
            return [vec[0] ?? 0, vec[1] ?? 0, vec[2] ?? 0, vec[3] ?? 0];
        }
        case "transform": {
            const rot = value.data.rotation;
            return [rot[0] ?? 0, rot[1] ?? 0, rot[2] ?? 0, rot[3] ?? 0];
        }
        case "enum":
            return normalizedValueAsQuat(value.data[1]);
        case "array":
        case "list":
        case "tuple":
            return value.data.length > 0
                ? normalizedValueAsQuat(value.data[0])
                : undefined;
        default:
            return undefined;
    }
}
function normalizedValueAsColorRgba(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "colorrgba":
            return [...value.data];
        case "vec4": {
            const [r, g, b, a] = value.data;
            return [r, g, b, a];
        }
        case "vector": {
            const vec = value.data;
            return [vec[0] ?? 0, vec[1] ?? 0, vec[2] ?? 0, vec[3] ?? 0];
        }
        case "float": {
            const num = Number(value.data) || 0;
            return [num, num, num, 1];
        }
        case "bool":
            return value.data ? [1, 1, 1, 1] : [0, 0, 0, 1];
        case "transform": {
            const scale = value.data.scale;
            return [scale[0] ?? 0, scale[1] ?? 0, scale[2] ?? 0, 1];
        }
        case "enum":
            return normalizedValueAsColorRgba(value.data[1]);
        case "array":
        case "list":
        case "tuple":
            return value.data.length > 0
                ? normalizedValueAsColorRgba(value.data[0])
                : undefined;
        default:
            return undefined;
    }
}
function normalizedValueAsText(value) {
    const type = coerceNormalizedType(value);
    if (!type)
        return undefined;
    switch (type) {
        case "text":
            return value.data;
        case "enum":
            return normalizedValueAsText(value.data[1]);
        case "array":
        case "list":
        case "tuple":
            return value.data.length > 0
                ? normalizedValueAsText(value.data[0])
                : undefined;
        default:
            return undefined;
    }
}
function legacyValueAsNumber(value) {
    if (value == null)
        return undefined;
    if (typeof value === "number") {
        return Number.isFinite(value) ? value : undefined;
    }
    if (typeof value === "boolean") {
        return value ? 1 : 0;
    }
    if (Array.isArray(value) && value.length > 0) {
        return legacyValueAsNumber(value[0]);
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        // Already normalized, handled elsewhere.
        return undefined;
    }
    if ("float" in value)
        return legacyValueAsNumber(value.float);
    if ("bool" in value)
        return value.bool ? 1 : 0;
    if ("vec2" in value)
        return legacyValueAsNumber(value.vec2?.[0]);
    if ("vec3" in value)
        return legacyValueAsNumber(value.vec3?.[0]);
    if ("vec4" in value)
        return legacyValueAsNumber(value.vec4?.[0]);
    if ("quat" in value)
        return legacyValueAsNumber(value.quat?.[0]);
    if ("color" in value)
        return legacyValueAsNumber(value.color?.[0]);
    if ("vector" in value) {
        const vec = value.vector;
        return Array.isArray(vec) && vec.length > 0 ? legacyValueAsNumber(vec[0]) : undefined;
    }
    if ("transform" in value) {
        const transform = value.transform;
        if (isRecord(transform)) {
            const translation = transform.translation ?? transform.pos;
            if (Array.isArray(translation) && translation.length > 0) {
                return legacyValueAsNumber(translation[0]);
            }
        }
        return undefined;
    }
    if ("enum" in value)
        return legacyValueAsNumber(value.enum?.value);
    if ("array" in value) {
        const arr = value.array;
        return Array.isArray(arr) && arr.length > 0 ? legacyValueAsNumber(arr[0]) : undefined;
    }
    if ("list" in value) {
        const list = value.list;
        return Array.isArray(list) && list.length > 0 ? legacyValueAsNumber(list[0]) : undefined;
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        return Array.isArray(tuple) && tuple.length > 0 ? legacyValueAsNumber(tuple[0]) : undefined;
    }
    if ("data" in value)
        return legacyValueAsNumber(value.data);
    if ("value" in value)
        return legacyValueAsNumber(value.value);
    return undefined;
}
function legacyValueAsNumericArray(value, fallback) {
    if (value == null)
        return undefined;
    if (typeof value === "number") {
        return [Number.isFinite(value) ? value : fallback];
    }
    if (typeof value === "boolean") {
        return [value ? 1 : 0];
    }
    if (Array.isArray(value)) {
        return value.map((entry) => toFiniteNumber(entry) ?? fallback);
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("float" in value)
        return legacyValueAsNumericArray(value.float, fallback);
    if ("bool" in value)
        return legacyValueAsNumericArray(value.bool, fallback);
    if ("vec2" in value)
        return coerceNumericArray(value.vec2, fallback);
    if ("vec3" in value)
        return coerceNumericArray(value.vec3, fallback);
    if ("vec4" in value)
        return coerceNumericArray(value.vec4, fallback);
    if ("quat" in value)
        return coerceNumericArray(value.quat, fallback);
    if ("color" in value)
        return coerceNumericArray(value.color, fallback);
    if ("vector" in value)
        return coerceNumericArray(value.vector, fallback);
    return undefined;
}
function legacyValueAsTransform(value) {
    if (value == null || !isRecord(value))
        return undefined;
    if (!("transform" in value))
        return undefined;
    const payload = value.transform;
    if (!isRecord(payload))
        return undefined;
    const translation = coerceTuple(payload.translation ?? payload.pos, DEFAULT_VEC3) ?? DEFAULT_VEC3.slice();
    const rotation = coerceTuple(payload.rotation ?? payload.rot, DEFAULT_QUAT) ?? DEFAULT_QUAT.slice();
    const scale = coerceTuple(payload.scale, DEFAULT_SCALE) ?? DEFAULT_SCALE.slice();
    if (!translation || !rotation || !scale)
        return undefined;
    return {
        translation: translation,
        rotation: rotation,
        scale: scale,
    };
}
function legacyValueAsVec3(value) {
    if (value == null)
        return undefined;
    if (typeof value === "number") {
        return [value, value, value];
    }
    if (typeof value === "boolean") {
        return value ? [1, 1, 1] : [0, 0, 0];
    }
    if (Array.isArray(value)) {
        return [value[0] ?? 0, value[1] ?? 0, value[2] ?? 0];
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("vec3" in value)
        return value.vec3;
    if ("vec4" in value) {
        const vec4 = value.vec4;
        return [vec4[0], vec4[1], vec4[2]];
    }
    if ("quat" in value) {
        const quat = value.quat;
        return [quat[0], quat[1], quat[2]];
    }
    if ("color" in value) {
        const color = value.color;
        return [color[0], color[1], color[2]];
    }
    if ("vector" in value) {
        const vec = value.vector;
        return [vec[0] ?? 0, vec[1] ?? 0, vec[2] ?? 0];
    }
    if ("transform" in value) {
        const payload = value.transform;
        if (isRecord(payload)) {
            const pos = payload.translation ?? payload.pos ?? DEFAULT_VEC3;
            return [pos[0] ?? 0, pos[1] ?? 0, pos[2] ?? 0];
        }
    }
    if ("enum" in value)
        return legacyValueAsVec3(value.enum?.value);
    if ("array" in value) {
        const array = value.array;
        return Array.isArray(array) && array.length > 0 ? legacyValueAsVec3(array[0]) : undefined;
    }
    if ("list" in value) {
        const list = value.list;
        return Array.isArray(list) && list.length > 0 ? legacyValueAsVec3(list[0]) : undefined;
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        return Array.isArray(tuple) && tuple.length > 0 ? legacyValueAsVec3(tuple[0]) : undefined;
    }
    if ("float" in value)
        return legacyValueAsVec3(value.float);
    if ("bool" in value)
        return legacyValueAsVec3(value.bool);
    return undefined;
}
function legacyValueAsVector(value) {
    if (value == null)
        return undefined;
    if (typeof value === "number") {
        return Number.isFinite(value) ? [value] : undefined;
    }
    if (typeof value === "boolean") {
        return [value ? 1 : 0];
    }
    if (Array.isArray(value)) {
        return value.map((entry) => toFiniteNumber(entry) ?? 0);
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("vector" in value) {
        const vec = value.vector;
        return Array.isArray(vec) ? vec.map((entry) => toFiniteNumber(entry) ?? 0) : undefined;
    }
    if ("vec2" in value)
        return [...(value.vec2 ?? DEFAULT_VEC2)];
    if ("vec3" in value)
        return [...(value.vec3 ?? DEFAULT_VEC3)];
    if ("vec4" in value)
        return [...(value.vec4 ?? DEFAULT_VEC4)];
    if ("quat" in value)
        return [...(value.quat ?? DEFAULT_QUAT)];
    if ("color" in value)
        return [...(value.color ?? DEFAULT_COLOR)];
    if ("transform" in value) {
        const payload = value.transform;
        if (isRecord(payload)) {
            const pos = payload.translation ?? payload.pos ?? DEFAULT_VEC3;
            const rot = payload.rotation ?? payload.rot ?? DEFAULT_QUAT;
            const scale = payload.scale ?? DEFAULT_SCALE;
            return [
                pos[0] ?? 0,
                pos[1] ?? 0,
                pos[2] ?? 0,
                rot[0] ?? 0,
                rot[1] ?? 0,
                rot[2] ?? 0,
                rot[3] ?? 0,
                scale[0] ?? 1,
                scale[1] ?? 1,
                scale[2] ?? 1,
            ];
        }
    }
    if ("enum" in value)
        return legacyValueAsVector(value.enum?.value);
    if ("array" in value) {
        const arr = value.array;
        if (Array.isArray(arr)) {
            return arr.flatMap((entry) => legacyValueAsVector(entry) ?? []);
        }
    }
    if ("list" in value) {
        const list = value.list;
        if (Array.isArray(list)) {
            return list.flatMap((entry) => legacyValueAsVector(entry) ?? []);
        }
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        if (Array.isArray(tuple)) {
            return tuple.flatMap((entry) => legacyValueAsVector(entry) ?? []);
        }
    }
    if ("float" in value) {
        const num = legacyValueAsNumber(value.float);
        return num !== undefined ? [num] : undefined;
    }
    if ("bool" in value) {
        return value.bool ? [1] : [0];
    }
    return undefined;
}
function legacyValueAsBool(value) {
    if (value == null)
        return undefined;
    if (typeof value === "boolean")
        return value;
    if (typeof value === "number")
        return value !== 0;
    if (typeof value === "string")
        return value.length > 0;
    if (Array.isArray(value)) {
        return value.some((entry) => (toFiniteNumber(entry) ?? 0) !== 0);
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("bool" in value)
        return value.bool;
    if ("float" in value)
        return (value.float ?? 0) !== 0;
    if ("text" in value)
        return (value.text ?? "").length > 0;
    if ("vector" in value) {
        const vec = value.vector;
        return Array.isArray(vec) ? vec.some((entry) => entry !== 0) : undefined;
    }
    if ("vec2" in value)
        return value.vec2?.some((entry) => entry !== 0);
    if ("vec3" in value)
        return value.vec3?.some((entry) => entry !== 0);
    if ("vec4" in value)
        return value.vec4?.some((entry) => entry !== 0);
    if ("quat" in value)
        return value.quat?.some((entry) => entry !== 0);
    if ("color" in value)
        return value.color?.some((entry) => entry !== 0);
    if ("transform" in value) {
        const payload = value.transform;
        if (isRecord(payload)) {
            const translation = payload.translation ?? payload.pos ?? [];
            const rotation = payload.rotation ?? payload.rot ?? [];
            const scale = payload.scale ?? [];
            return (translation.some((entry) => entry !== 0) ||
                rotation.some((entry) => entry !== 0) ||
                scale.some((entry) => entry !== 0));
        }
        return undefined;
    }
    if ("enum" in value)
        return legacyValueAsBool(value.enum?.value) ?? false;
    if ("record" in value) {
        const recordVal = value.record;
        return Object.values(recordVal ?? {}).some((entry) => legacyValueAsBool(entry) ?? false);
    }
    if ("array" in value) {
        const arr = value.array;
        return Array.isArray(arr) ? arr.some((entry) => legacyValueAsBool(entry) ?? false) : undefined;
    }
    if ("list" in value) {
        const list = value.list;
        return Array.isArray(list) ? list.some((entry) => legacyValueAsBool(entry) ?? false) : undefined;
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        return Array.isArray(tuple) ? tuple.some((entry) => legacyValueAsBool(entry) ?? false) : undefined;
    }
    if ("data" in value)
        return legacyValueAsBool(value.data);
    if ("value" in value)
        return legacyValueAsBool(value.value);
    return undefined;
}
function legacyValueAsQuat(value) {
    if (value == null)
        return undefined;
    if (Array.isArray(value)) {
        return [value[0] ?? 0, value[1] ?? 0, value[2] ?? 0, value[3] ?? 0];
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("quat" in value)
        return value.quat;
    if ("vec4" in value)
        return value.vec4;
    if ("vector" in value) {
        const vec = value.vector;
        return [vec[0] ?? 0, vec[1] ?? 0, vec[2] ?? 0, vec[3] ?? 0];
    }
    if ("transform" in value) {
        const payload = value.transform;
        if (isRecord(payload)) {
            const rot = payload.rotation ?? payload.rot ?? DEFAULT_QUAT;
            return [rot[0] ?? 0, rot[1] ?? 0, rot[2] ?? 0, rot[3] ?? 0];
        }
    }
    if ("enum" in value)
        return legacyValueAsQuat(value.enum?.value);
    if ("array" in value) {
        const arr = value.array;
        return Array.isArray(arr) && arr.length > 0 ? legacyValueAsQuat(arr[0]) : undefined;
    }
    if ("list" in value) {
        const list = value.list;
        return Array.isArray(list) && list.length > 0 ? legacyValueAsQuat(list[0]) : undefined;
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        return Array.isArray(tuple) && tuple.length > 0 ? legacyValueAsQuat(tuple[0]) : undefined;
    }
    return undefined;
}
function legacyValueAsColorRgba(value) {
    if (value == null)
        return undefined;
    if (Array.isArray(value)) {
        return [value[0] ?? 0, value[1] ?? 0, value[2] ?? 0, value[3] ?? 0];
    }
    if (typeof value === "number") {
        return [value, value, value, 1];
    }
    if (typeof value === "boolean") {
        return value ? [1, 1, 1, 1] : [0, 0, 0, 1];
    }
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("bool" in value)
        return value.bool ? [1, 1, 1, 1] : [0, 0, 0, 1];
    if ("float" in value) {
        const num = value.float ?? 0;
        return [num, num, num, 1];
    }
    if ("color" in value)
        return value.color;
    if ("vec4" in value)
        return value.vec4;
    if ("vector" in value) {
        const vec = value.vector;
        return [vec[0] ?? 0, vec[1] ?? 0, vec[2] ?? 0, vec[3] ?? 0];
    }
    if ("transform" in value) {
        const payload = value.transform;
        if (isRecord(payload)) {
            const scale = payload.scale ?? DEFAULT_SCALE;
            return [scale[0] ?? 0, scale[1] ?? 0, scale[2] ?? 0, 1];
        }
    }
    if ("enum" in value)
        return legacyValueAsColorRgba(value.enum?.value);
    if ("array" in value) {
        const arr = value.array;
        return Array.isArray(arr) && arr.length > 0 ? legacyValueAsColorRgba(arr[0]) : undefined;
    }
    if ("list" in value) {
        const list = value.list;
        return Array.isArray(list) && list.length > 0 ? legacyValueAsColorRgba(list[0]) : undefined;
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        return Array.isArray(tuple) && tuple.length > 0 ? legacyValueAsColorRgba(tuple[0]) : undefined;
    }
    return undefined;
}
function legacyValueAsText(value) {
    if (value == null)
        return undefined;
    if (typeof value === "string")
        return value;
    if (!isRecord(value))
        return undefined;
    if ("type" in value && "data" in value) {
        return undefined;
    }
    if ("text" in value)
        return value.text ?? EMPTY_STRING;
    if ("enum" in value)
        return legacyValueAsText(value.enum?.value);
    if ("array" in value) {
        const arr = value.array;
        return Array.isArray(arr) && arr.length > 0 ? legacyValueAsText(arr[0]) : undefined;
    }
    if ("list" in value) {
        const list = value.list;
        return Array.isArray(list) && list.length > 0 ? legacyValueAsText(list[0]) : undefined;
    }
    if ("tuple" in value) {
        const tuple = value.tuple;
        return Array.isArray(tuple) && tuple.length > 0 ? legacyValueAsText(tuple[0]) : undefined;
    }
    if ("data" in value)
        return legacyValueAsText(value.data);
    if ("value" in value)
        return legacyValueAsText(value.value);
    return undefined;
}
/**
 * Normalize primitive JS values/primitives into the ValueJSON surface.
 * Arrays are encoded as generic vectors to avoid implicit vec2/vec3 coercions.
 */
export function toValueJSON(value) {
    if (typeof value === "number") {
        return { float: value };
    }
    if (typeof value === "boolean") {
        return { bool: value };
    }
    if (typeof value === "string") {
        return { text: value };
    }
    if (Array.isArray(value)) {
        return { vector: value.slice() };
    }
    return value;
}
export function isNormalizedValue(value) {
    return typeof value === "object" && value !== null && "type" in value && "data" in value;
}
export function valueAsNumber(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsNumber(value);
    }
    return legacyValueAsNumber(value);
}
export function valueAsNumericArray(value, fallback = 0) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsNumericArray(value, fallback);
    }
    return legacyValueAsNumericArray(value, fallback);
}
export function valueAsTransform(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsTransform(value);
    }
    return legacyValueAsTransform(value);
}
export function valueAsVec3(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsVec3(value);
    }
    return legacyValueAsVec3(value);
}
export function valueAsVector(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsVector(value);
    }
    return legacyValueAsVector(value);
}
export function valueAsBool(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsBool(value);
    }
    return legacyValueAsBool(value);
}
export function valueAsQuat(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsQuat(value);
    }
    return legacyValueAsQuat(value);
}
export function valueAsColorRgba(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsColorRgba(value);
    }
    return legacyValueAsColorRgba(value);
}
export function valueAsText(value) {
    if (value == null)
        return undefined;
    if (isNormalizedValue(value)) {
        return normalizedValueAsText(value);
    }
    return legacyValueAsText(value);
}
